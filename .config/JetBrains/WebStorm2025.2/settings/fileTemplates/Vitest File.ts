import { describe, it, expect } from "vitest"
import { ${NAME} } from "./${NAME}.ts"

describe("${NAME}", () => {
  it("should", () => {
    // Arrange
    #[[$END$]]#
    // Act
    // Assert
  })
})